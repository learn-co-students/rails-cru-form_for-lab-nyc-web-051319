<h1>Genre Index</h1>

<ul>
  <%@genres.each do |genre|%>
    <li>
      <%=link_to genre.name, genre_path(genre)%>
    </li>
  <%end%>
</ul>

<%=button_to "New Genre", new_genre_path, method: "get"%>
