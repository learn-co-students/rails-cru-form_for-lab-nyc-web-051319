<h1>Artist Index</h1>

<ul>
  <%@artists.each do |artist|%>
  <li>
    <%=link_to artist.name, artist_path(artist)%>
  </li>
  <%end%>
</ul>

<%=button_to "New Artist", new_artist_path, method: "get"%>
